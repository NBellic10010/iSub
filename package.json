{
  "name": "isub-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "iSub monorepo — Move contracts, @isubpay/sdk, and the web frontend share one install so @mysten/sui/react resolve to a single copy.",
  "workspaces": [
    "sdk",
    "web",
    "checkout"
  ],
  "dependencies": {
    "@isubpay/sdk": "^0.0.1",
    "@mysten/sui": "^2.20.0"
  }
}
